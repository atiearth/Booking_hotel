<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Booking</title>
    <link rel="stylesheet" type="text/css" href="/css/adminstyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <h1 class="mt-5 text-center">Booking Details</h1>
        <div class="card mt-4">
            <div class="card-header">
                <h4>Booking Information</h4>
            </div>
            <div class="card-body">
                <p><strong>Booking ID:</strong> <span th:text="${booking.bookingId}"></span></p>
                <p><strong>Room Type:</strong> <span th:text="${booking.room.type}"></span></p>
                <p><strong>Floor:</strong> <span th:text="${booking.room.floor}"></span></p>
                <p><strong>Room Number:</strong> <span th:text="${booking.room.number}"></span></p>
               <!--  <p><strong>Booking Date:</strong> <span th:text="${#dates.format(booking.bookingDate, 'dd/MM/yyyy')}"></span></p>
                <p><strong>Check-in Date:</strong> <span th:text="${#dates.format(booking.checkinDate, 'dd/MM/yyyy')}"></span></p>
                <p><strong>Check-out Date:</strong> <span th:text="${#dates.format(booking.checkoutDate, 'dd/MM/yyyy')}"></span></p>
                <p><strong>Payment Type:</strong> <span th:text="${booking.paymentType}"></span></p>
                <p><strong>Total Price:</strong> <span th:text="${booking.totalPrice}"></span></p> -->
                <a href="/view/bookings" class="btn btn-secondary">Back to Bookings</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
